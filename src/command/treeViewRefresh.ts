/**
 * 刷新树组件
 */
import { Memento, ExtensionContext, window } from 'vscode';

export default async function treeViewRefresh(
    context: ExtensionContext,
    state: Memento,
    uri: any,
    intl: { get: (key: string) => string }
) {

}
